//  ofxVoxel.cpp
//  Created by Jordi on 22/04/14.
#include "ofxVoxel.h"


//----------------------------------------------------------
ofxVoxel::ofxVoxel()
//: x(position.x), y(position.y), z(position.z),
//w(size.x), h(size.y), d(size.z){
	{
//	set(0,0,0,0,0,0);
//	pix.set(255);
	cout <<"Voxel constructed"<<endl;
	cout <<"pix."<<endl;
		pix.get
}

//----------------------------------------------------------
ofxVoxel::~ ofxVoxel(){
}
/*
//----------------------------------------------------------
ofxVoxel::ofxVoxel(float px, float py, float pz, float sw, float sh, float sd)
: x(position.x), y(position.y), z(position.z),
w(size.x), h(size.y), d(size.z){
	set(px,py,pz,sw,sh,sd);
}

//----------------------------------------------------------
ofxVoxel::ofxVoxel(const ofPoint& p, float sw, float sh, float sd)
: x(position.x), y(position.y), z(position.z),
w(size.x), h(size.y), d(size.z){
	set(p,sw,sh,sd);
}

//----------------------------------------------------------
ofxBox::ofxVoxel(const ofxBox& box)
: x(position.x), y(position.y), z(position.z),
w(size.x), h(size.y), d(size.z){
	set(box);
}

//----------------------------------------------------------
ofxBox::ofxBox(const ofPoint& p, const ofPoint& s)
: x(position.x), y(position.y), z(position.z),
w(size.x), h(size.y), d(size.z){
	set(p,s);
}
*/